# Configuration

**Example Definition:**

```
protected $fields = [
    'example' => [
        'type'   => 'anomaly.field_type.colorpicker',
        'config' => [
            'default_value' => '#333333'
        ]
    ]
];
```

### `default_value`

The default color for the colorpicker.
